<%= form_with(model: vehicle, url:"vehicles/new_from_api") do |form| %>
<div class="card">
  <div class="card-body">
    <% if vehicle.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

        <ul>
          <% vehicle.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :vin, style: "display: block" %>
      <%= form.text_field :vin %>
    </div>

    <div>
      <%= form.submit 'Search VIN', class: "btn btn-primary" %>
    </div>
  </div>
</div>
<% end %>
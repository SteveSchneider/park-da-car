# README
   ## running from clone:
   ```
   rails db:migrate
   rails db:seed
   rails rails css:install:bootstrap
   rails javascript:install:esbuild
   rails assets:precompile
   rails server
   ```